export interface IOrders {
    // tslint:disable-next-line: align
        productName: string;
        productCode: string;
        purchaseDate: string;
        price: number;
        starRating: number;
}
